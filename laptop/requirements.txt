# Laptop-side runtime + test dependencies (Python 3.10+).
# The core protocol (sensorstream.protocol) is pure stdlib; these add the
# receiver server, discovery, numerics and tests.
websockets>=12.0
zeroconf>=0.132
numpy>=1.24
pytest>=7.4
