"""sensorstream — laptop-side receiver, sync, sinks and dashboard for the
Real-Time Mobile Sensor Streaming system.

The wire protocol implemented in :mod:`sensorstream.protocol` is the single
source of truth shared with the Android app (see ``docs/protocol.md``).
"""

__version__ = "0.1.0"
